@using Models
@typeparam TItem

<thead>
	<tr>
		@foreach (var column in Columns)
		{
			<th class="clickable">@(column).Label</th>
		}
	</tr>
</thead>

@code{
	[Parameter]
	public IReadOnlyList<TItem> Columns { get; set; }
	[Parameter]
	public IReadOnlyList<TItem> Data { get; set; }
}